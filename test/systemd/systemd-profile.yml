---
name: SystemdTest
hostname: systemdyacmtest

# Here you can define any custom made executable scripts
# that you would like to have run before moving on to
# package installation.
# initial_scripts:

package_manager: dnf
packages:
  - httpd

# Here you can define any custom made executable scripts
# that you would like to have run before moving on to
# enabling services.
# midway_scripts:

init_system: systemd
services:
  - httpd

# Here you can define any custom made executable scripts
# that you would like to have run as a final bootstrap
# step.
final_scripts:
  - check-systemd.sh
