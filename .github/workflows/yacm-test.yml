name: Test yacm

on:
  push:
    branches:
      - actions-test

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      - name: run tests
        run: cd test/ubuntu && ./run-tests.sh
